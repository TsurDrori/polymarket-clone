# Session 12 — Sports Custom Layout

Status: `blocked-on-08A`
Tags: `sports`, `custom-layout`, `league-filters`, `list-rows`, `games-vs-futures`
Depends on: `05`, `08A`, `10`
Touches: `features/sports/*`, `app/sports/*`, sports sub-routes
Review docs: current Next.js docs if nested route conventions changed; Gamma findings if sports payload shape assumptions need verification; current public Polymarket sports surfaces; Session `08R` output
Done when: `/sports` becomes a populated, futures-first sports surface aligned to the current live Polymarket sports page, with league filters and row-based markets
Commit target: `feat(sports): add futures-first sports surface with league filters and rows`

**Goal:** `/sports` becomes a dedicated sports surface with league filters and row-based markets. Game-row treatment is optional and should only ship if the fetched slice actually supports it.

Review gate: Session `08R` found that the rogue `SportsHub` defaulted to an empty `Live` state because the current top-volume sports slice is futures-heavy. On 2026-04-19, the top `30` sports events yielded `25` visible events and the first `12` were all outrights/futures. This session must start from a populated default route, not from a sportsbook layout assumption.

- [ ] `features/sports/parse.ts`
  - [ ] Keep league extraction from tags
  - [ ] Treat `future/outright` vs `game` as the first classification decision
  - [ ] Only classify moneyline / spread / total inside a game-specific subsection if the data supports it
- [ ] `features/sports/components/SportsFilterBar.tsx`
  - [ ] Futures-first default with league filters and counts
  - [ ] Add a games toggle only if the fetched slice can support a non-empty experience
- [ ] `features/sports/components/SportsMarketRow.tsx`
  - [ ] Compact row with title/teams, primary price via `<PriceCell>`, volume, end date, and league badge
- [ ] `features/sports/components/SportsMarketList.tsx`
  - [ ] League-grouped or single-feed rows, whichever better matches the live surface after `08A`
- [ ] `app/sports/page.tsx`
  - [ ] Render a populated default sports surface
  - [ ] No empty live tab on first load
- [ ] Sub-routes
  - [ ] Only add nested `live` / league routes if they clearly earn their keep after current-data verification
- [ ] If a game-heavy subsection is implemented, wire moneyline / spread / total parsing there rather than forcing the whole page into that layout
- [ ] Loading skeleton matches row/list layout
- [ ] QA at `1280×800`, `1024×768`, `600×960`, and `375×812` against the current live sports page
- [ ] **Commit:** `feat(sports): add futures-first sports surface with league filters and rows`
