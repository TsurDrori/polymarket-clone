# Session 10 — WebSocket Subscriptions And Live UI

Status: `complete-with-manual-qa-follow-up`
Tags: `websocket`, `subscriptions`, `pricecell`, `live-prices`, `flash-animation`, `profiler`
Depends on: `09`, `08`
Touches: `features/realtime/subscriptions.ts`, `features/events/components/PriceCell.tsx`, `features/detail/components/MarketRow.tsx`, realtime tests, live QA flows
Review docs: [polymarket-api-findings.md](/Users/tsur/projects/polymarket-clone/project-notes/polymarket-api-findings.md:205)
Done when: prices tick live on home, category pages, and detail page, the detail probability bar stays in sync with the live yes price, and reconnects replay subscriptions cleanly
Commit target: `feat(realtime): wire live websocket prices with ref-counted subscriptions`

**Goal:** Prices tick live on home, category pages, and detail page. The detail page's percentage text and probability bar stay in sync. Flash animation is visible. Reconnects cleanly and resumes subscriptions automatically.

Automated verification on 2026-04-19: `pnpm test -- --run`, `pnpm lint`, and `pnpm build` all passed. Manual browser QA and profiler capture are still pending.

- [x] `features/realtime/subscriptions.ts`:
  - [x] Ref-counted `Map<tokenId, count>`
  - [x] Key ref counts by unique token ID so repeated `PriceCell` mounts for the same token do not duplicate socket subscriptions
  - [x] `subscribe(tokenIds)` increments counts, de-dupes input, diffs the active unique-token set, and sends WS updates only when the effective subscription set changes; use the documented dynamic subscribe behavior
  - [x] `unsubscribe(tokenIds)` decrements, removes when count hits `0`, and only sends the documented unsubscribe message for tokens that truly left the active set
  - [x] Debounce WS sends to 50ms to coalesce mount storms
  - [x] On socket open or reopen, immediately replay the current active token set and verify the transport heartbeat path stays active so subscriptions recover after Wi-Fi drops without a page refresh
  - [x] Start with the simple per-`PriceCell` lifecycle subscription model by default; only add viewport gating if profiler evidence on the large detail-page stress case shows the simple strategy exceeds budget
  - [x] Viewport gating was intentionally not added in this pass; if profiler evidence later proves it necessary, gate at the row/card level rather than per-cell `IntersectionObserver` wiring
- [x] `PriceCell` `useEffect`: `subscribe([tokenId])` on mount, `unsubscribe([tokenId])` on unmount
- [x] Detail-page live parity:
  - [x] The detail `ProbabilityBar` reads from the same live yes-token state as the headline percentage so the bar and text move together
  - [x] Keep the live subscription boundary granular: do not make the whole `MarketRow` rerender on each tick just to animate the bar
- [x] Tests:
  - [x] Add unit coverage for duplicate subscribe/unsubscribe calls, ref-count churn, and 50ms send coalescing
  - [x] Add reconnect coverage proving the active token set is replayed after a socket reopen
  - [x] Add a regression test for the detail live UI path so the probability bar and percentage stay aligned for the same token
- [ ] Manual QA:
  - [ ] Home tickers: flash animations visible, no layout shift, numbers don't jitter (thanks `tabular-nums`)
  - [ ] FIFA detail page: 60-market stress test, no UI jank
  - [ ] Detail page: probability text and bar stay visually aligned while prices move
  - [ ] Confirm the chosen subscription strategy still behaves well on the 60-market page: either simple lifecycle subscriptions remain within budget, or measured viewport gating kicks in without flicker or stale rows
  - [ ] Wi-Fi disconnect → reconnect cycle works, active subscriptions replay automatically, and prices resume without manual navigation
  - [ ] Chrome DevTools → React Profiler / Performance tracks → confirm tick updates stay localized to `PriceCell` and any dedicated live bar child rather than rerendering whole cards or rows
- [x] **Commit:** `feat(realtime): wire live websocket prices with ref-counted subscriptions`
