# Session 11 — Crypto Custom Layout

Status: `blocked-on-08A`
Tags: `crypto`, `custom-layout`, `filters`, `list-rows`, `classification`
Depends on: `05`, `08A`, `10`
Touches: `features/crypto/*`, `app/crypto/*`
Review docs: current Next.js docs if route/file conventions changed; Gamma findings if crypto payload shape assumptions need verification; current public Polymarket crypto surfaces; Session `08R` output
Done when: `/crypto` replaces the safe baseline with a current-Polymarket-style, list-first crypto surface that only ships filters the live data can actually support
Commit target: `feat(crypto): add list-first crypto surface with data-backed filters and rows`

**Goal:** `/crypto` becomes a dedicated crypto surface with compact filter pills, list-first market rows, and data-backed grouping that reflects the current live Polymarket crypto page better than either the rogue card hub or the older grouped-expander plan.

Review gate: Session `08R` found that the rogue `CryptoExplorer` relied on filters that are mostly unsupported by the current live top-volume crypto slice. On 2026-04-19, the top-30 visible crypto feed resolved to `pre-market: 17`, `all: 13`, market-type `all: 30`, and most asset chips collapsed into `all`. This session must build from observed data and question patterns, not from hoped-for tags.

- [ ] `features/crypto/parse.ts`
  - [ ] Derive time bucket from tags when present
  - [ ] Derive lead asset from visible tags and title heuristics
  - [ ] Derive market family from question text (`above`, `hit`, `FDV`, `airdrop`, etc.) rather than assuming stable Gamma market-type tags
- [ ] `features/crypto/components/CryptoFilterBar.tsx`
  - [ ] Render only the pill rows that have real counts in the fetched slice
  - [ ] No dead chip rows or speculative controls
- [ ] `features/crypto/components/CryptoMarketRow.tsx`
  - [ ] Compact row layout with icon, title, primary outcome price via `<PriceCell>`, volume/meta, and secondary label
- [ ] `features/crypto/components/CryptoMarketList.tsx`
  - [ ] List-first layout, optionally grouped by asset or active time bucket
  - [ ] Do not return to the rogue 4-up card hub
- [ ] `app/crypto/page.tsx`
  - [ ] Render the dedicated crypto surface from the parsed facets and row list
  - [ ] Keep server fetch + `Hydrator` contract intact
- [ ] Sub-routes
  - [ ] Only add nested routes if the live surface and current data justify them after `08A`
  - [ ] Do not pre-commit to `5M`, `15M`, `bitcoin`, etc pages just because the older plan named them
- [ ] Loading skeleton matches row/list layout
- [ ] QA at `1280×800`, `1024×768`, `600×960`, and `375×812` against the current live crypto page
- [ ] **Commit:** `feat(crypto): add list-first crypto surface with data-backed filters and rows`
