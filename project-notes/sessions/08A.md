# Session 08A — Surface Realignment Before Realtime

Status: `next`
Tags: `refactor`, `home`, `shell`, `category-baseline`, `parity`, `event-grid`
Depends on: `06`, `07`, `08R`
Touches: `app/{page,layout,politics,crypto,sports}/*`, `features/{home,categories,crypto,sports}/*`, `features/events/components/EventGrid.tsx`, related CSS modules
Review docs: Session `08R`, current live Polymarket home/category surfaces, installed Next 16 docs for server/client boundaries
Done when: the shipped rogue surfaces are realigned to a safe, parity-first baseline and the app is ready for realtime work without stale client-boundary or empty-route regressions
Commit target: `refactor(shell): realign home and category surfaces before realtime`

**Goal:** keep the right product shape from the rogue work, but refactor the implementation so the app looks and behaves closer to current Polymarket before Session `09` starts pushing live updates through it.

## Implementation Checklist

- [ ] `features/events/components/EventGrid.tsx`
  - [ ] Remove `'use client'`
  - [ ] Keep it as a pure server-rendered layout wrapper so `EventCard` stays the real client boundary
- [ ] `app/layout.tsx`, `features/categories/components/{Header,Footer}.*`
  - [ ] Keep the shared shell global and sticky
  - [ ] Refactor the header and footer toward the lighter 2026-04-19 live Polymarket chrome
  - [ ] Replace the forced dark baseline with system-pref-driven theming so the app follows `prefers-color-scheme` by default, matching live Polymarket
  - [ ] Do not ship dead browse pills, fake auth UI, or oversized footer chrome just to mimic density
- [ ] `features/home/HomePage.tsx` + `features/home/*`
  - [ ] Keep the accepted four-module homepage composition: hero, breaking news, hot topics, explore all
  - [ ] Refactor the section styling and spacing toward the current live homepage
  - [ ] Remove arbitrary local-only hide toggles unless they map to real, visible product controls
  - [ ] Turn `Hot topics` into a compact ranked module rather than a large chip wall
- [ ] `features/categories/CategoryPage.tsx` + `features/categories/CategoryPage.module.css`
  - [ ] Rework politics and other baseline category framing toward a lighter list/grid-first surface
  - [ ] Avoid dense hero-like framing or oversized topic-pill treatment
- [ ] `app/politics/page.tsx`
  - [ ] Update copy and framing so the page reads like a current Polymarket category route, not a bespoke dashboard
- [ ] `app/crypto/page.tsx` and `app/sports/page.tsx`
  - [ ] Remove `CryptoExplorer` and `SportsHub` as the active top-level route entrypoints
  - [ ] Replace them with safe, populated baseline category surfaces that continue to fetch on the server and hydrate once
  - [ ] The sports route must never default to an empty `Live` state again
- [ ] Rogue-only helpers/components
  - [ ] Delete or demote helpers/components that are no longer the accepted target instead of leaving dead code behind
- [ ] Manual QA
  - [ ] Compare local vs live home, politics, crypto, and sports on `1280×800`, `1024×768`, `600×960`, and `375×812`
  - [ ] Confirm header, footer, and page framing hold up on both desktop and mobile
  - [ ] Confirm the top-level `/sports` and `/crypto` routes are populated and defensible again
- [ ] **Commit:** `refactor(shell): realign home and category surfaces before realtime`
