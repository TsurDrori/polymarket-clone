# Session 07 — Header, Category Nav, And Category Routes

Status: `complete-with-realignment`
Tags: `header`, `navigation`, `category-routes`, `server-filtering`, `pathname`
Depends on: `02`, `06`
Touches: `features/categories/*`, `app/layout.tsx`, `app/{politics,sports,crypto}/*`, `features/events/api/gamma.ts`
Review docs: Gamma findings for `tag_slug`; local Next.js docs for App Router nav semantics if behavior changed
Done when: top nav routes to category pages and each category fetches its own server-side Gamma slice
Commit target: `feat(nav): add sticky header and per-category routes with server-side tag filtering`

**Goal:** Top nav with 4 tabs; clicking a tab navigates to `/politics`, `/sports`, `/crypto`; each category page fetches its own Gamma slice server-side.

Realignment note (2026-04-19): Session `08R` kept the important Session `07` decisions — route segment as category source of truth, server-side `tag_slug` filtering, a shared persistent layout shell, and mobile-safe category navigation. What changed is the accepted shell target: the app should keep a richer global shell than the original minimal 4-tab header, but the currently landed dark chrome and dense category framing are provisional and must be refactored in Session `08A`.

Reference for the routing decision: the route is the only source of truth for category state. Do not introduce a `categoryAtom` or any URL-sync effect here.

Payload note from live Gamma checks on 2026-04-19: `listEvents({ limit: 30, order: 'volume_24hr' })` filtered client-side yields only **2 sports events** (and 7 crypto, 15 politics). A `/sports` grid with 2 cards looks broken. Gamma supports `tag_slug=politics` as a server-side filter — use it. Client-side filtering stays only for the `isEventVisible` pass from Session 6.

- [x] `features/events/api/gamma.ts` — extend `ListEventsParams` with optional `tagSlug?: string`; `buildListUrl` serializes `params.set("tag_slug", tagSlug)` when present. Keep `listEvents` signature additive; homepage call stays unchanged.
- [x] `features/categories/components/Header.tsx` (+ `.module.css`) — **server component** (no client hooks), `position: sticky; top: 0; z-index: 10;`, `background: var(--bg-page); border-bottom: 1px solid var(--border);`, logo left, `<CategoryNav>` right. Logo = inline SVG purple-P on `#15191D` square (ship a placeholder now; Session 15 replaces/tunes alongside the favicon). Height ~56px.
- [x] `features/categories/components/CategoryNav.tsx` (+ `.module.css`) — **client component** (`'use client'` for `usePathname`). 4 tabs: `Trending` (`/`), `Politics` (`/politics`), `Sports` (`/sports`), `Crypto` (`/crypto`). Wrapper is a plain `<nav aria-label="Categories">` — **not** `role="tablist"` (tab/tablist pattern is wrong for links that navigate to different URLs). Default `<Link>` prefetch stays on.
- [x] `features/categories/activeTab.ts` — pure helper `isTabActive(pathname: string, href: string): boolean`:
  - `href === '/'` → `pathname === '/'`
  - else → `pathname === href || pathname.startsWith(href + '/')`
  - One unit test locking in the three cases above
- [x] Tab primitive wiring: when `<Link>` is active, pass **both** `aria-current="page"` and `aria-selected={true}`
- [x] `app/politics/page.tsx`, `app/sports/page.tsx`, `app/crypto/page.tsx` — each RSC:
  - `await listEvents({ tagSlug: 'politics', limit: 30, order: 'volume_24hr', ascending: false })` (and `'sports'` / `'crypto'` respectively)
  - Apply `isEventVisible` filter
  - Render `<><Hydrator events={visible}/><EventGrid events={visible}/></>`
  - Empty state + error state inherited from `app/error.tsx` / inline empty branch
- [x] `app/politics/loading.tsx`, `app/sports/loading.tsx`, `app/crypto/loading.tsx` — re-export homepage `loading.tsx` content (12-card skeleton grid)
- [x] Active tab animated underline already lives in `shared/ui/Tab.module.css` via `[aria-selected="true"]::after` — no new CSS
- [x] Header rendered once in `app/layout.tsx` above `{children}` so it persists across all routes including `/event/[slug]`
- [x] Drop the `features/categories/filter.ts` plan entry — with server-side `tag_slug`, the category routes no longer filter client-side
- [x] Visual QA at 1280×800 and 375×812 — header 57px, 4 tabs fit both widths (P-logo-only at 375px), animated underline, back/forward restores active tab. Card counts with the corrected `isEventVisible`: `/` 29, `/politics` 22, `/sports` 10, `/crypto` 29. QA surfaced two real bugs, both fixed in Session 7:
  - `isEventVisible` was filtering on `tag.forceHide`, which hid every category event because Gamma flags the headline tags (`sports`, `politics`, `crypto`) as `forceHide: true`. Narrowed to the `hide-from-new` slug only; `forceHide` stays a tag-chip visibility flag via `getVisibleTags`.
  - Active `<Tab>` was only emitting `aria-current="page"` — added `aria-selected={true}` alongside so the dual-attribute contract holds even if the CSS later depends on only one.
- [x] **Commit:** `feat(nav): add sticky header and per-category routes with server-side tag filtering`
